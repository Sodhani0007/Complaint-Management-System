# Used by `docker compose up` — copy this to `.env` in the project root and
# fill in your real key. Per-service .env files (backend/.env.example) are
# for running services outside Docker; this one only needs the secret that
# docker-compose.yml injects into the backend container.

GROQ_API_KEY=your_groq_api_key_here
